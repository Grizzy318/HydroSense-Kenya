import pandas as pd

weather = pd.read_csv("weather_daily.csv")

# Convert date column
weather['date'] = pd.to_datetime(weather['date'])

# Fill missing values with median
weather['rainfall_mm'] = weather['rainfall_mm'].fillna(
    weather['rainfall_mm'].median()
)

weather['humidity_pct'] = weather['humidity_pct'].fillna(
    weather['humidity_pct'].median()
)

# Remove duplicates
weather = weather.drop_duplicates()

# Save cleaned dataset
weather.to_csv("weather_daily_cleaned.csv", index=False)
